import { colors } from './colors';

export const shareModalStyles = {
  header: {
    backgroundColor: colors.primary[2],
  },
  content: {
    backgroundColor: colors.primary[2],
    border: `solid 3px ${colors.primary[5]}`,
    borderRadius: 12,
  },
};
